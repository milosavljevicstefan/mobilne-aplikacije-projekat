<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" />
    <stroke
        android:width="2dp"
        android:color="@color/gray" /> <!-- Adjust the border color -->
    <corners android:radius="10dp" /> <!-- Adjust the radius as needed -->
</shape>
